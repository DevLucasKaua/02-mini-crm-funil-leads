import type { Lead } from '../../lib/types'

interface LeadDrawerProps {
  lead: Lead | null
  onClose: () => void
}

// Stub — implementado na Fase 3
export function LeadDrawer(_props: LeadDrawerProps) {
  return null
}
